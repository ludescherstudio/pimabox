# Clean URL: meinedomain.at/pimabox → stats.php
RewriteEngine On
RewriteRule ^pimabox$ pimabox.php [L]

# Block direct access to config and cache
<Files "config.php">
    Order Allow,Deny
    Deny from all
</Files>

# Recommended: prevent directory listing
Options -Indexes
